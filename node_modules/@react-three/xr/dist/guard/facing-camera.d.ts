import { ReactNode } from 'react';
import { Vector3 } from 'three';
/**
 * guard that only shows its shildren if the camera towards the object based on the provided angle and direction
 */
export declare function ShowIfFacingCamera({ children, direction, angle, }: {
    children?: ReactNode;
    direction: Vector3;
    angle?: number;
}): import("react/jsx-runtime").JSX.Element;
/**
 * guard that only renders its shildren if the camera towards the object based on the provided angle and direction
 */
export declare function IfFacingCamera({ children, direction, angle, }: {
    children?: ReactNode;
    direction: Vector3;
    angle?: number;
}): import("react/jsx-runtime").JSX.Element | null;
