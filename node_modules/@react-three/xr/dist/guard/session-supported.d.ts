import { ReactNode } from 'react';
/**
 * guard that only show its children if the session mode is supported
 */
export declare function ShowIfSessionModeSupported({ children, mode }: {
    children?: ReactNode;
    mode: XRSessionMode;
}): import("react/jsx-runtime").JSX.Element;
/**
 * guard that only renders its visible if the session mode is supported
 */
export declare function IfSessionModeSupported({ children, mode }: {
    children?: ReactNode;
    mode: XRSessionMode;
}): import("react/jsx-runtime").JSX.Element | null;
